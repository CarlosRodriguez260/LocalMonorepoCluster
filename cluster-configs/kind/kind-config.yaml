kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 443
    hostPort: 8080  # Map Argo CD's HTTPS port to localhost:8080
    protocol: TCP
  - containerPort: 80
    hostPort: 8081  # For potential ingress
    protocol: TCP